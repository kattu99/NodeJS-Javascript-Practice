/* Anytime you create a new object you can create an object factory. You create function instead and this just creates new objects instead of just the old one*/

module.exports = function () {
    return {
        favMovie: ""
    }
}
